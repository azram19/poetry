# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{app/public/.+\.(css|js|html)})
  watch(%r{app/public/css/.+\.(css|js|html)})
  watch(%r{app/public/js/.+\.(css|js|html)})
  watch(%r{app/views/.+\.(hbs)})
  # Rails Assets Pipeline
end
